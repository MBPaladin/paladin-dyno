behaviors:

  # A grid search actuator efficency test
  - id: EFFICIENCY
    type: grid_search
    settings:
      input_motor:
        control_mode: velocity
        command_list: [15, 30, 60, 100] # units = Rad/s
      output_motor:
        control_mode: torque
        command_list: [-5, -10, -20, -40, 5, 10, 20, 40]
      loop_order: [velocity, torque]
      duration_per_point_s: 3
      settle_time_s: 0.5
      transition_rate: 0.25 #Fraction of max allowable velocity / torque ramp rate