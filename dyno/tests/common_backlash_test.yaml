behaviors:
  # A backlash measurement test that runs up to 10 Nm
  - id: BACKLASH
    type: test_trace
    settings:
      input_motor:
        control_mode: torque
      output_motor:
        control_mode: position
      trace_file: "backlash_trace.csv"